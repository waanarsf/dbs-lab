LAB_1

I.CREATE TABLE EMPLOYEE(
  EMP_NO INT,
  EMP_NAME VARCHAR(50),
  EMP_ADDRESS VARCHAR(50));
II. INSERT INTO EMPLOYEE(EMP_NO,EMP_NAME,EMP_ADDRESS)VALUES(11,'KJ','MANIPAL');
   INSERT INTO EMPLOYEE(EMP_NO,EMP_NAME,EMP_ADDRESS)VALUES(12,'CJ','MANGLORE');
   INSERT INTO EMPLOYEE(EMP_NO,EMP_NAME,EMP_ADDRESS)VALUES(14,'RJ','MANGLORE');
   INSERT INTO EMPLOYEE(EMP_NO,EMP_NAME,EMP_ADDRESS)VALUES(13,'MJ','UDUPI');
   INSERT INTO EMPLOYEE(EMP_NO,EMP_NAME,EMP_ADDRESS)VALUES(10,'KJ','MANIPAL');

III. SELECT "EMP_NAME" FROM EMPLOYEE;

IV. SELECT * FROM EMPLOYEE WHERE "EMP_ADDRESS" = 'MANIPAL';

V. ALTER TABLE EMPLOYEE ADD("SALARY" NUMBER(6,2));

VI. UPDATE EMPLOYEE SET "SALARY" = '2000';

VII DESCRIBE EMPOYEE;

VIII. DELETE FROM EMPLOYEE WHERE "EMP_ADDRESS" = 'MANGLORE';

IX.RENAME EMPLOYEE TO EMPLOYEE1;

X.DROP TABLE EMPLOYEE1;